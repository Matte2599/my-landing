---
import { t } from '../../i18n/utils';
import type { Locale } from '../../i18n/ui';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---

<a class="skip-link" href="#main">{t('skip.toContent', locale)}</a>
